---
export interface Props {
	text?: string;
	placeholder?: string;
	name?: string;
	type?: any;
}

const { text, placeholder, name, type } = Astro.props;
---

<input
	placeholder={placeholder}
	value={text}
	name={name}
	type={type || "text"}
/>

<br />
<style>
	input {
		border: none;
		background-color: whitesmoke;
		border-radius: 5px;
		margin: 1rem;
		padding: 0.7rem;
		font-size: 0.8rem;
	}
	input:focus {
		outline: none;
	}
</style>
